alias ll='ls -lah'
alias gs='git status'
alias ..='cd ..'

# Brew
alias update_brew='brew update; brew upgrade; brew upgrade --cask; brew cleanup'
alias clean_brew="brew autoremove -v && brew cleanup -s --prune=all -v"
